columns=3
rows=3
# Nine 341px frames in one 1023x1023 square. Held 150ms each, same pace as
# `default`, so this animation simply runs longer because it has more movement
# in it -- nine frames is 1350ms against default six frames at 900ms.
frameDurationMillis=150
