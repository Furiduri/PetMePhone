columns=3
rows=3
# Nine 341px frames in one 1023x1023 square, held 250ms each: 2250ms for the full
# animation. Slower per frame than `default`, which is the point of declaring this
# per character rather than sharing one global rate.
frameDurationMillis=250
