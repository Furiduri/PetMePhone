columns=3
rows=3
# Nine 341px frames in one 1023x1023 square, looping in 900ms -- 100ms a frame.
# `default` is six frames in the same 900ms, so the two loop at the same pace with
# different smoothness, which is the whole point of declaring a cycle rather than
# a frame interval.
durationMillis=900
