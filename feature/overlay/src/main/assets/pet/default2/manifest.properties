columns=6
rows=2
# One full loop in 900ms, matching `default`. Without this, 12 frames at the
# configured 150ms each take 1.8s -- the same pace reading as sluggish purely
# because this sheet has twice the frames.
durationMillis=900
