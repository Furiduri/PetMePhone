package com.petmephone

/**
 * Sole owner of the build values shared across every module. No module or convention plugin
 * declares a literal duplicate of any of these — they read them from here.
 */
object ProjectConfig {
    const val compileSdk = 37
    const val minSdk = 30
    const val targetSdk = 37
    const val jvmToolchain = 17
}
